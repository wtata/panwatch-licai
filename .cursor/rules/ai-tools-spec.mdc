---
description: '自研工具开发规范 v1.1（适用于 D:\AI-Tools\ 下所有自研工具）'
alwaysApply: true
---

<!-- 来源：D:\AI-Tools\自研工具开发规范.md，版本 v1.1 -->

# 自研工具开发规范 v1.1

改动记录：
- v1.1（2026-09-24）：根据首轮检查结果微调：复刻的开源项目也放私有仓库；不带 HTTP 服务的桌面客户端免健康检查；允许用 Electron safeStorage 存密钥；已有迁移工具的项目沿用原工具。
- v1.0（2026-09-24）：第一版定稿。

使用方式：新建或修改任何自研工具前先读本规范并遵守；给出建议或方案时也以本规范为准。规范需要调整时，涉及产品和使用方式的先和王涛确认，纯代码层面的细节可以直接调整后告知他；调整后更新本技能和 `D:\AI-Tools\自研工具开发规范.md`，各项目的 `.cursor/rules/ai-tools-spec.mdc` 同步更新，并在改动记录里加一条。

适用范围：`D:\AI-Tools\` 下的所有自研工具。新工具从第一天起遵守；旧工具安排一轮集中补齐（见第九节）。

## 一、原则
- 技术不限。Electron、网页、Python、小程序、浏览器扩展都可以，按场景选。本规范只管「怎么接入、怎么管理」，不管「用什么写」。
- 本机优先。先在本机跑通、验收，再考虑上云。GitHub 用来备份和记录版本。
- 每个版本都能用。宁可功能少，也不留跑不起来的半成品。

## 二、目录与命名
- 每个工具放在 `D:\AI-Tools\<英文目录名>`，目录名用小写字母加短横线，例如 `ai-idea-board`。
- 每个工具有一个中文名（界面上显示）和一个英文名（目录、代码、仓库），两者一一对应，登记在工具箱清单里。
- GitHub 仓库名与英文目录名相同，放在 `wtata` 下，默认私有。
- 从开源项目复刻来的工具也要放私有仓库：新建同名私有仓库，搬入完整历史，保留原项目的 `LICENSE` 和版权声明，另配一个 `upstream` 远程用来同步原作者的更新。

## 三、接入工具箱的五条硬规矩
1. **登记**：在 `D:\AI-Tools\my-toolbox\tools.json` 里加一条，至少写清中文名、英文名、目录、启动命令、访问地址（有的话）、对应的 GitHub 仓库。字段以 `tools.json` 现有格式为准。
2. **健康检查**：有后端或常驻服务的工具，提供 `GET /health`，正常时返回 `{"status":"ok","version":"1.2.0"}`，异常时 `status` 为 `"error"` 并附一句 `message`。纯前端页面、浏览器扩展和不带 HTTP 服务的桌面客户端可以免这一条，由工具箱检查进程或文件。
3. **版本号**：格式为「主版本.次版本.修订号」，例如 `1.2.0`。版本号只在一个地方维护（`package.json` 的 `version`，或根目录的 `VERSION` 文件），`/health` 返回同一个值。每次发版打一个 git 标签，例如 `v1.2.0`。
4. **日志**：写到各项目自己的 `logs\` 目录，按天分文件，`logs\` 加入 `.gitignore`。工具箱通过 `tools.json` 里登记的目录找到它。线上服务的日志位置写进 README。
5. **账号**：需要登录的工具接入统一账号服务（见第十节），不再各自做注册登录；用户数据按用户 ID 隔离，前端本地缓存也按用户 ID 分开存。统一账号服务上线前，新工具先不做登录，或只做本机单用户。
   - **内置管理员**：需要登录的工具，都统一有一个内置的管理员账号，能管理用户、会员和工具内的数据。管理员账号在服务第一次启动时自动创建；账号和初始密码从环境变量读取，不写进代码，首次登录后必须修改密码。统一账号服务上线后，由它统一提供这个管理员身份，各工具从登录凭证里识别管理员权限。

## 四、数据与运行环境
- **本地文件数据**：数据存成本地文件（JSON、CSV、图片等，不用数据库）的工具，数据统一放在项目的 `data\` 目录，不进 Git，由工具箱定期复制备份到另一个位置。用数据库的工具不在这条里。
- **运行环境版本**：每个项目写明 Node 和 Python 版本；Python 项目用各自的虚拟环境；依赖锁定版本（`package-lock.json`、带版本号的 `requirements.txt` 等）。

## 五、安全
- 密钥、token、密码不写进代码、不写进提交到 Git 的配置文件。本机放 Windows 凭据管理器、Electron 的 `safeStorage` 加密存储，或 `.env`（`.env` 必须在 `.gitignore` 里），线上放服务器环境变量。
- 需要把密钥交给助手时，走安全录入，不贴进聊天。
- 远程重启、重新部署、删除数据这类操作，执行前必须确认一次。

## 六、每个项目必备的文件
- `README.md`：一句话说明它是干什么的、怎么启动、用哪个端口、依赖什么、线上地址（有的话）。
- `.gitignore`：排除密钥、日志、构建产物、`node_modules`、Python 虚拟环境。
- 启动只需要一条命令，这条命令同时写在 README 和 `tools.json` 里。

## 七、端口与部署
- 本机端口统一登记在 `tools.json`，新工具先查一遍再选，避免冲突。
- 已知占用：股视盯 `8000`（Docker），点子看板线上 `8088`。
- 线上服务统一用 Docker 部署，每个仓库根目录带同名部署脚本（例如 `deploy.sh`），工具箱的一键重新部署只调用这一条命令。
- 镜像在服务器上直接构建，或放阿里云自己的镜像仓库，不依赖 Docker Hub 私有仓库。
- 新增线上服务前，先看一眼服务器剩余内存，够不够再加一个容器。

## 八、给助手和 Cursor 的约定
- 用中文沟通；代码注释中英文都可以。
- 改代码前先读本规范；新建工具时，先按第二、三、四、六节搭好骨架，再写功能。
- 本机优先改代码并热更新验收，GitHub 只做备份同步，不默认走云端代理。
- 每次改动后，确认 `tools.json`、README、版本号三处是否需要同步更新。
- 用数据库的项目，表结构变更用迁移工具记录（新项目用 Alembic；已有迁移工具的项目沿用原工具），线上升级按迁移执行，不手动改表。

## 九、旧工具集中补齐
- 本规范定稿后，安排一轮集中补齐，逐个工具对照第二、三、四、六节检查并补上缺的部分。
- 补齐顺序：先线上服务（点子看板、股视盯、超了没），再本机常用工具，最后其余工具。
- 每个工具补齐后在工具箱里验收：服务页能看到状态和版本号，日志能打开。

## 十、统一账号服务
结论：不复用点子看板现有的登录，另建一个独立的账号服务。点子看板的登录只服务它自己：只有短信验证码，没有微信登录、没有密码、没有刷新和吊销，也没法区分「某个用户能用哪些工具」，补齐这些等于重做。

- 形态：独立的 FastAPI 服务，单独部署，单独数据库（MySQL）。
- 登录方式：手机号为主身份，支持短信验证码、密码、微信小程序登录，网页和小程序可以互相绑定同一个账号。
- 凭证：短期访问令牌加刷新令牌，退出登录时服务端吊销。令牌里写明发给哪个工具，各工具只认发给自己的令牌。
- 权限：会员等级（VIP）全局统一；另有一张表记录每个用户能用哪些工具、有什么权限。
- 各工具的业务数据仍存在各自的库里，只用统一账号的用户 ID 关联。
- 可以直接借用的现成代码：点子看板的短信限流和 VIP 字段，超了没的密码哈希（PBKDF2 加盐）和微信登录流程。
- 迁移：点子看板和超了没的现有用户按手机号合并到统一账号，两边改成调用统一服务登录。这一步属于账号服务项目本身，不算在第九节的集中补齐里。
- 安全底线：签名密钥只放环境变量；登录接口加频率限制。

## 十一、对外开放
- 任何工具对外开放（给别人用或收费）之前，单独评估合规：用户协议、隐私政策、手机号等个人信息的存储和使用，微信小程序的上架要求。

## 十二、规范的维护
- 文档开头记录版本号和改动记录。
- 每次集中补齐或新项目做完后回头检查一次：规范不合适就改规范，不让代码悄悄偏离规范。
